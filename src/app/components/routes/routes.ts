import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-routes',
  imports: [RouterModule],
  templateUrl: './routes.html',
  styleUrl: './routes.css',
})
export class RoutesDemo {}
